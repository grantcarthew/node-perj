const perj = require('../src/perj')
const Writer = require('./writer')
const writer = new Writer()
const write = writer.write.bind(writer)
const passThrough = true

let log = perj.create({ test: 'parent', write, passThrough })
let child = []

describe('child separator', () => {
  test('parent', () => {
    log.info('parent', { parent: true })
    expect(writer.jsonOut.level).toBe('info')
    expect(writer.jsonOut.lvl).toBe(30)
    expect(writer.getType(writer.jsonOut.time)).toBe('Number')
    expect(writer.jsonOut.msg).toBe('parent')
    expect(writer.jsonOut.data.parent).toBe(true)
    expect(writer.jsonOut.test).toBe('parent')
  })
  test('first child', () => {
    child.push(log.child({ test: 'first child' }))
    child[0].info('first child', { c1: true })
    expect(writer.jsonOut.level).toBe('info')
    expect(writer.jsonOut.lvl).toBe(30)
    expect(writer.getType(writer.jsonOut.time)).toBe('Number')
    expect(writer.jsonOut.msg).toBe('first child')
    expect(writer.jsonOut.data.c1).toBe(true)
    expect(writer.jsonOut.test).toBe('parent > first child')
  })
  test('second child', () => {
    child.push(child[0].child({ test: 'second child' }))
    child[1].info('second child', { c2: true })
    expect(writer.jsonOut.level).toBe('info')
    expect(writer.jsonOut.lvl).toBe(30)
    expect(writer.getType(writer.jsonOut.time)).toBe('Number')
    expect(writer.jsonOut.msg).toBe('second child')
    expect(writer.jsonOut.data.c2).toBe(true)
    expect(writer.jsonOut.test).toBe('parent > first child > second child')
  })
  test('third child', () => {
    child.push(child[1].child({ test: 'third child' }))
    child[2].info('third child', { c3: true })
    expect(writer.jsonOut.level).toBe('info')
    expect(writer.jsonOut.lvl).toBe(30)
    expect(writer.getType(writer.jsonOut.time)).toBe('Number')
    expect(writer.jsonOut.msg).toBe('third child')
    expect(writer.jsonOut.data.c3).toBe(true)
    expect(writer.jsonOut.test).toBe('parent > first child > second child > third child')
  })
  test('parent unchanged', () => {
    log.info('parent', { parent: true })
    expect(writer.jsonOut.level).toBe('info')
    expect(writer.jsonOut.lvl).toBe(30)
    expect(writer.getType(writer.jsonOut.time)).toBe('Number')
    expect(writer.jsonOut.msg).toBe('parent')
    expect(writer.jsonOut.data.parent).toBe(true)
    expect(writer.jsonOut.test).toBe('parent')
  })
})

describe('custom child separator', () => {
  test('custom parent', () => {
    log = perj.create({ test: 'parent', write, passThrough, separator: ':' })
    child = []
    expect(writer.jsonOut.level).toBe('info')
    expect(writer.jsonOut.lvl).toBe(30)
    expect(writer.getType(writer.jsonOut.time)).toBe('Number')
    expect(writer.jsonOut.msg).toBe('parent')
    expect(writer.jsonOut.data.parent).toBe(true)
    expect(writer.jsonOut.test).toBe('parent')
  })
  test('custom first child', () => {
    child.push(log.child({ test: 'first child' }))
    child[0].info('first child', { c1: true })
    expect(writer.jsonOut.level).toBe('info')
    expect(writer.jsonOut.lvl).toBe(30)
    expect(writer.getType(writer.jsonOut.time)).toBe('Number')
    expect(writer.jsonOut.msg).toBe('first child')
    expect(writer.jsonOut.data.c1).toBe(true)
    expect(writer.jsonOut.test).toBe('parent:first child')
  })
  test('custom second child', () => {
    child.push(child[0].child({ test: 'second child' }))
    child[1].info('second child', { c2: true })
    expect(writer.jsonOut.level).toBe('info')
    expect(writer.jsonOut.lvl).toBe(30)
    expect(writer.getType(writer.jsonOut.time)).toBe('Number')
    expect(writer.jsonOut.msg).toBe('second child')
    expect(writer.jsonOut.data.c2).toBe(true)
    expect(writer.jsonOut.test).toBe('parent:first child:second child')
  })
  test('custom third child', () => {
    child.push(child[1].child({ test: 'third child' }))
    child[2].info('third child', { c3: true })
    expect(writer.jsonOut.level).toBe('info')
    expect(writer.jsonOut.lvl).toBe(30)
    expect(writer.getType(writer.jsonOut.time)).toBe('Number')
    expect(writer.jsonOut.msg).toBe('third child')
    expect(writer.jsonOut.data.c3).toBe(true)
    expect(writer.jsonOut.test).toBe('parent:first child:second child:third child')
  })
  test('custom parent unchanged', () => {
    log.info('parent', { parent: true })
    expect(writer.jsonOut.level).toBe('info')
    expect(writer.jsonOut.lvl).toBe(30)
    expect(writer.getType(writer.jsonOut.time)).toBe('Number')
    expect(writer.jsonOut.msg).toBe('parent')
    expect(writer.jsonOut.data.parent).toBe(true)
    expect(writer.jsonOut.test).toBe('parent')
  })
})

describe('object no child separator', () => {
  test('parent no child separator', () => {
    log = perj.create({ test: { p: 1 }, write, passThrough, separator: ':' })
    child = []
    log.info('parent', { parent: true })
    expect(writer.jsonOut.level).toBe('info')
    expect(writer.jsonOut.lvl).toBe(30)
    expect(writer.getType(writer.jsonOut.time)).toBe('Number')
    expect(writer.jsonOut.msg).toBe('parent')
    expect(writer.jsonOut.data.parent).toBe(true)
    expect(writer.jsonOut.test.p).toBe(1)
  })
  test('first child no child separator', () => {
    child.push(log.child({ test: { c: 1 } }))
    child[0].info('first child', { c1: true })
    expect(writer.jsonOut.level).toBe('info')
    expect(writer.jsonOut.lvl).toBe(30)
    expect(writer.getType(writer.jsonOut.time)).toBe('Number')
    expect(writer.jsonOut.msg).toBe('first child')
    expect(writer.jsonOut.data.c1).toBe(true)
    expect(writer.jsonOut.test.c).toBe(1)
  })
  test('second child no child separator', () => {
    child.push(child[0].child({ test: { c: 2 } }))
    child[1].info('second child', { c2: true })
    expect(writer.jsonOut.level).toBe('info')
    expect(writer.jsonOut.lvl).toBe(30)
    expect(writer.getType(writer.jsonOut.time)).toBe('Number')
    expect(writer.jsonOut.msg).toBe('second child')
    expect(writer.jsonOut.data.c2).toBe(true)
    expect(writer.jsonOut.test.c).toBe(2)
  })
  test('parent unchanged no child separator', () => {
    log.info('parent', { parent: true })
    expect(writer.jsonOut.level).toBe('info')
    expect(writer.jsonOut.lvl).toBe(30)
    expect(writer.getType(writer.jsonOut.time)).toBe('Number')
    expect(writer.jsonOut.msg).toBe('parent')
    expect(writer.jsonOut.data.parent).toBe(true)
    expect(writer.jsonOut.test.p).toBe(1)
  })
})

describe('deep child separator', () => {
  let deepLog = perj.create({ test: 'p', write, passThrough, separator: '#' })
  let value = 'p'
  test('parent deep', () => {
    deepLog.info(value, { parent: true })
    expect(writer.jsonOut.level).toBe('info')
    expect(writer.jsonOut.lvl).toBe(30)
    expect(writer.getType(writer.jsonOut.time)).toBe('Number')
    expect(writer.jsonOut.msg).toBe('p')
    expect(writer.jsonOut.data.parent).toBe(true)
    expect(writer.jsonOut.test).toBe(value)
  })
  test('child deep separator', () => {
    for (let i = 0; i < 4; i++) {
      value += '#c' + i
      deepLog = deepLog.child({ test: 'c' + i })
      deepLog.info('c' + i, { c: i })
      expect(writer.jsonOut.level).toBe('info')
      expect(writer.jsonOut.lvl).toBe(30)
      expect(writer.getType(writer.jsonOut.time)).toBe('Number')
      expect(writer.jsonOut.msg).toBe('c' + i)
      expect(writer.jsonOut.data.c).toBe(i)
      expect(writer.jsonOut.test).toBe(value)
    }
  })
})
